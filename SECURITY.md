# Security policy

This repository is documentation only. Do not submit real credentials, proxy subscriptions, node details, account identifiers, or raw diagnostic logs in issues or pull requests.

If a secret is committed, revoke or rotate it immediately. Removing the visible file is not sufficient because the value remains in Git history.

